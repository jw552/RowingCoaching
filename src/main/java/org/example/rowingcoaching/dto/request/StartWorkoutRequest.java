package org.example.rowingcoaching.dto.request;

import lombok.Getter;
import lombok.Setter;

@Getter
@Setter
public class StartWorkoutRequest {
    // This can be empty for now, but provides a structure for future enhancements
    // like setting initial parameters, warm-up preferences, etc.
} 